import 'package:flutter/material.dart'; 

void main() async { 
  /*
    1. Login page --> store username and hash of password 
      --> Finite state machine that we'll get from storage 
        --> If they're authenticated, app'll default go to homepage
            otherwise, go to login page. Maybe fb auth? 
  */
  runApp(MaterialApp( 
    
  )); 
}